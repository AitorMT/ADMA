node_modules
npm-debug.log
Dockerfile
.dockerignore
.git
.gitignore
# Local env files — secrets must NEVER be baked into the image.
# The only env var accepted at build time is VITE_API_BASE_URL (via ARG).
.env
.env.local
.env.*.local
dist/